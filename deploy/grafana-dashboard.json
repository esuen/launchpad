{
  "dashboard": {
    "title": "Launchpad",
    "uid": "launchpad",
    "timezone": "browser",
    "refresh": "10s",
    "panels": [
      {
        "title": "Request Rate (req/s)",
        "type": "timeseries",
        "datasource": {"type": "prometheus", "uid": ""},
        "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
        "targets": [
          {
            "datasource": {"type": "prometheus", "uid": ""},
            "expr": "sum(rate(http_requests_total{app=\"launchpad\"}[2m])) by (path)",
            "legendFormat": "{{path}}",
            "refId": "A"
          }
        ],
        "fieldConfig": {
          "defaults": {
            "unit": "reqps"
          }
        }
      },
      {
        "title": "Request Latency p50 / p95 / p99",
        "type": "timeseries",
        "datasource": {"type": "prometheus", "uid": ""},
        "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
        "targets": [
          {
            "datasource": {"type": "prometheus", "uid": ""},
            "expr": "histogram_quantile(0.50, sum(rate(http_request_duration_seconds_bucket{app=\"launchpad\"}[2m])) by (le))",
            "legendFormat": "p50",
            "refId": "A"
          },
          {
            "datasource": {"type": "prometheus", "uid": ""},
            "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket{app=\"launchpad\"}[2m])) by (le))",
            "legendFormat": "p95",
            "refId": "B"
          },
          {
            "datasource": {"type": "prometheus", "uid": ""},
            "expr": "histogram_quantile(0.99, sum(rate(http_request_duration_seconds_bucket{app=\"launchpad\"}[2m])) by (le))",
            "legendFormat": "p99",
            "refId": "C"
          }
        ],
        "fieldConfig": {
          "defaults": {
            "unit": "s"
          }
        }
      },
      {
        "title": "Error Rate (5xx)",
        "type": "timeseries",
        "datasource": {"type": "prometheus", "uid": ""},
        "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8},
        "targets": [
          {
            "datasource": {"type": "prometheus", "uid": ""},
            "expr": "sum(rate(http_requests_total{app=\"launchpad\", status=~\"5..\"}[2m])) or vector(0)",
            "legendFormat": "5xx errors/s",
            "refId": "A"
          }
        ],
        "fieldConfig": {
          "defaults": {
            "unit": "reqps"
          }
        }
      },
      {
        "title": "Requests by Status Code",
        "type": "timeseries",
        "datasource": {"type": "prometheus", "uid": ""},
        "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8},
        "targets": [
          {
            "datasource": {"type": "prometheus", "uid": ""},
            "expr": "sum(rate(http_requests_total{app=\"launchpad\"}[2m])) by (status)",
            "legendFormat": "{{status}}",
            "refId": "A"
          }
        ],
        "fieldConfig": {
          "defaults": {
            "unit": "reqps"
          }
        }
      },
      {
        "title": "Active Pods",
        "type": "stat",
        "datasource": {"type": "prometheus", "uid": ""},
        "gridPos": {"h": 4, "w": 6, "x": 0, "y": 16},
        "targets": [
          {
            "datasource": {"type": "prometheus", "uid": ""},
            "expr": "count(up{app=\"launchpad\"} == 1)",
            "refId": "A"
          }
        ]
      },
      {
        "title": "Total Requests",
        "type": "stat",
        "datasource": {"type": "prometheus", "uid": ""},
        "gridPos": {"h": 4, "w": 6, "x": 6, "y": 16},
        "targets": [
          {
            "datasource": {"type": "prometheus", "uid": ""},
            "expr": "sum(http_requests_total{app=\"launchpad\"})",
            "refId": "A"
          }
        ]
      }
    ],
    "schemaVersion": 39
  },
  "overwrite": true
}
